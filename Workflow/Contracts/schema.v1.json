{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ue-ai-cli.local/schemas/ue.workflow.v1.json",
  "title": "UE Workflow DSL v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "dsl",
    "dslVersion",
    "workflowKind",
    "workflowId",
    "scope",
    "operations"
  ],
  "properties": {
    "dsl": {
      "const": "ue.workflow"
    },
    "dslVersion": {
      "const": "1.0"
    },
    "workflowKind": {
      "const": "assetEdit"
    },
    "workflowId": {
      "type": "string",
      "minLength": 1
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "kind",
        "asset"
      ],
      "properties": {
        "kind": {
          "enum": [
            "blueprint",
            "widgetBlueprint",
            "material"
          ]
        },
        "asset": {
          "type": "string",
          "pattern": "^/Game/"
        },
        "createIfMissing": {
          "type": "boolean"
        }
      }
    },
    "persistence": {
      "const": "dirtyOnly"
    },
    "operations": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/operation"
      }
    },
    "verify": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "compile": {
          "type": "boolean",
          "description": "Compatibility input. UE Workflow v1 normalizes this value to true; false does not disable the automatic compile finalizer."
        },
        "readBack": {
          "type": "array",
          "description": "Requested structural read-back detail. A missing or empty array is normalized to the non-empty default for the selected scope.",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        }
      }
    }
  },
  "$defs": {
    "binding": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "from",
        "path"
      ],
      "properties": {
        "from": {
          "type": "string",
          "minLength": 1
        },
        "path": {
          "type": "string",
          "pattern": "^/"
        }
      }
    },
    "operation": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "type",
        "params"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[A-Za-z][A-Za-z0-9_-]*$"
        },
        "type": {
          "type": "string",
          "minLength": 1
        },
        "params": {
          "type": "object"
        },
        "dependsOn": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        },
        "bindings": {
          "type": "object",
          "propertyNames": {
            "pattern": "^/"
          },
          "additionalProperties": {
            "$ref": "#/$defs/binding"
          }
        }
      }
    }
  }
}
